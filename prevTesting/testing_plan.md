ver 0.01:

1. Базовый код с двумерным массивом, взаимодействием с мышкой, и типами ландшавтов ✔

2. Процедурная генерация мира ✔

3. Обязательно разобраться с компиляцией и разделить код на модули ✔

4. Симуляция ✔

   1. Сушности ✔
   2. Еда ✔

5. Улучшение симуляции ✔

   1. Добавление потребностей, поиска еды и смерти от голода ✔
   2. Enum поведение и направления движения ✔

6. Логирование ✔

Рефакторинг и изменение кода:

   # Перенести хранение сущностей и карты мира из стека в кучу: ✔

      Сделать единую struct world, в ней хранить:
      Массив клеток в heap - не двумерный, а одномерный, но с координатами
      ----
      | Конкретно перенос из матрицы в одномерный. mapSizeX,Y - оставить. Координаты - по прежнему логические. Для получения клетки         обращаться в массив по линеаризированному индексу(см. https://en.wikipedia.org/wiki/Row-_and_column-major_order). ✔
      ----
      | Теперь надо встроить это в код и восстановить закомментированные блоки:
         Инициализация сущностей и еды ✔
         Отрисовка и обновление сущностей и еды ✔
         Генерация ✔

   # Сделать mapSize более глобальной, а не непривязанной переменной
   # То же самое каасается типов ландшафта
   # Сделать более гибкими и глобальными переменные типа entitiesNumber
   # Добавить функцию updateWorld в модуль world

Нововведния и расшерение:

   # Добавить items
      # Надо будет сделать отдельный struct с enum типом. Хранить массив таких структов в World. Сделать отдельный модуль items.h с функциями обновления. Сделать еду предметами.

   # Вылить проект в Github

Улучшение моментов:

   # Улучшить генерацию. Добавить биомы, эрозию, и вообще сделать больше аспектов в генерации

ver 0.0.2:
# Ограничить область видимости и отвественности модуля entities.h